# Server Configuration
PORT=3000
NODE_ENV=production

# Optional: Add any API keys or secrets here if needed
# API_KEY=your_api_key_here

# =====================================================
# FIREBASE ADMIN SDK CREDENTIALS (for Chat Server)
# =====================================================
# Get these from Firebase Console > Project Settings > Service Accounts
# Click "Generate New Private Key" to download JSON file

FIREBASE_PROJECT_ID=n2shop-69e37
FIREBASE_CLIENT_EMAIL=your-service-account@n2shop-69e37.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Note:
# 1. Keep the quotes around FIREBASE_PRIVATE_KEY
# 2. Keep the \n characters in the private key
# 3. NEVER commit the actual .env file to git (it's in .gitignore)

# =====================================================
# POSTGRESQL DATABASE (for PostgreSQL Chat)
# =====================================================
# This will be auto-populated by Render if you add PostgreSQL service
# Format: postgresql://username:password@host:5432/database

DATABASE_URL=postgresql://localhost:5432/n2store_chat
