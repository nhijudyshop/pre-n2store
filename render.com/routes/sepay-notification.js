// =====================================================
// SEPAY NOTIFICATION
// Telegram notifications, alert sending
// Currently a placeholder - no notification routes exist yet
// =====================================================

/**
 * Register notification routes on the given router
 * @param {express.Router} router - Express router instance
 * @param {Object} helpers - Shared helper functions
 */
function registerRoutes(router, helpers) {
    // No notification routes implemented yet
    // Future: Telegram alerts, email notifications, etc.
}

module.exports = {
    registerRoutes
};
