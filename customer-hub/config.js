const API_CONFIG = {
    TPOS_ODATA: 'https://api.tpos.vn/odata' // Example, replace with actual TPOS OData API if different
};

window.API_CONFIG = API_CONFIG; // Expose to global scope for ApiService
